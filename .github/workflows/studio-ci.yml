name: studio-ui-ci

on:
  pull_request:
    branches: [main, codex/guided-studio-ui-20260914]

permissions:
  contents: read

concurrency:
  group: studio-ui-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run ci
      - name: Build with Studio hidden
        run: bun run build
        env:
          VITE_STUDIO_ENABLED: 'false'
      - name: Validate Worker deployment with Studio hidden
        run: |
          test -s dist/index.html
          npx --yes wrangler@4 deploy --dry-run
          npx --yes wrangler@4 versions upload --dry-run
        env:
          WRANGLER_SEND_METRICS: 'false'
      - name: Build with Studio enabled
        run: bun run build
        env:
          VITE_STUDIO_ENABLED: 'true'
      - name: Validate Worker deployment with Studio enabled
        run: |
          test -s dist/index.html
          npx --yes wrangler@4 deploy --dry-run
          npx --yes wrangler@4 versions upload --dry-run
        env:
          WRANGLER_SEND_METRICS: 'false'
      - name: Save build for browser regression checks
        uses: actions/upload-artifact@v4
        with:
          name: studio-ui-build
          path: dist
          retention-days: 3
      - name: Formatting diagnostics
        if: failure()
        run: |
          bunx biome check --write src/lib/studio src/components/studio src/pages/GuidedStudio.tsx src/main.tsx src/components/Shell.tsx || true
          git diff --binary > studio-format.patch
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: studio-format-diagnostics
          path: studio-format.patch
          retention-days: 3
          if-no-files-found: ignore
